[build]
  command = "pnpm build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "10"
  NODE_ENV = "production"

# Ensure Next.js plugin handles all routing
[functions]
  node_bundler = "esbuild"

